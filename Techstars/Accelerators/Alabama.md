
```button
name Get Me
type note(<% tp.date.now("HH:MM") %>) template
action Log Template Note
templater true
```

```button
name Load Data
type note(<% tp.date.now("HH:MM") %>) template
action Log Template Note
templater true
```
